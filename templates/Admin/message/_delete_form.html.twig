<form method="post" action="{{ path('app_admin_messages_delete', {'id': message.id}) }}" onsubmit="return confirm('Êtes -vous sûr de votre choîx?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button class="btn btn-danger">X</button>
</form>
